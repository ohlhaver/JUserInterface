<h1><h2><span class="page_label">Edit <%= my_or_user %> Account</span></h1>
 
<div class = "about">
<% form_for @user, :url => user_path(@user) do |f| %>
  <%= f.error_messages %>
  <% unless @user.third_party? %>
    <%= render :partial => 'jurnalo_user', :object => f %>
  <% else %>
    <%= render :partial => 'third_party_user', :object => f %>  
  <% end %>
  <br/>
  <%= f.submit "Update" %>
<% end %>
</span>